# 工具（Tools）

- 工具（Tools）是让大模型“会调用函数”的能力，而不只是聊天。
- LLM 负责“决定要不要用这个工具”, tools 定义提供上下文。
- 为什么 Tool 参数一定要用 zod？
zod 用来严格约束参数结构和类型，防止 LLM 生成脏数据直接进入业务逻辑；同时它能自动生成可读的参数说明给模型，提高调用准确率；最后还能在运行时做校验，一旦参数不合法立即失败，避免隐蔽 Bug 和安全风险。‘’
- 