# SQLAlchemy + FastAPI 集成

将 SQLAlchemy 嵌入 FastAPI 的生命周期
——用依赖注入管理会话， Depends
用 Pydantic 分离输入输出， 入参校验前端提交数据 统一规范接口输入输出
让数据库操作"既安全又优雅"。

1. 核心问题：Session 放在哪里？

在 Web 应用中，每个请求需要自己的 Session。如果每个端点都写上面的代码，会非常冗余。FastAPI 的 依赖注入 + yield 提供了最优雅的解决方案。

只需要写一次，每个端点直接依赖注入 Session 即可。

2. 