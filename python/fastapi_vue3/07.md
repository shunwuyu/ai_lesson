# Body 传参

如果想要获得body 请求体形式传递的参数， 需要使用 pydantic 先定义一个包含所有字段的模型。

新建一个test_main.http 文件
装httpYac vscode 插件

```
POST http://localhost:8000/login
Content-Type: application/json

{
  "email": "xx@qq.com",
  "password": "123456"
}
```