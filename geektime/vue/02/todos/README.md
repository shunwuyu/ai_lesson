# TodoList


## TodoList需求

![](https://static001.geekbang.org/resource/image/0c/01/0c58c5219ecb61394110ccb848829c01.png?wh=1526x884)

它有一个输入框，供我们输入数据；下方有一个列表，显示着所有我们要做的事情。

清单应用虽然看起来简单，不过麻雀虽小，五脏俱全。

- 以前
demo.html
我们先要找到目标元素，然后再进行对应的修改 DOM API
Vue.js，首先就要进行思想的升级，也就是说，不要再思考页面的元素怎么操作，而是要思考数据是怎么变化的。
Vue 让前端开发者能够专注数据本身的操作，而数据和页面的同步问题，则交由 Vue 来负责。
这种机制正是 Vue 当初受到开发者青睐的一个重要原因。

## 清单页面的渲染

我们循环遍历 todos 这个数据， 每一条遍历的结果叫 todo，然后把这个数据渲染出来，这样页面就能显示一个列表了。

## 处理用户交互
Vue 的思维
- 监听用户的输入。在监听中，如果判断到用户的输入是回车的时候，那就执行一个函数。
  @来标记用户的交互，
  @click 是点击
  @keydown 是键盘敲下
- 在执行的这个函数内部把 title 追加到 todos 最后面的位置，并且清空 title。
数据业务

## 额外信息的显示
我们想实现标记清单中某一项是否完成的功能。
而我们想要实现的标记功能，却是把列表中的某一项，用灰色的字体背景和中划线来标记，以此表示这一条内容是已经完成的内容。
对数据结构进行一下改造，把内容的数据类型，从简单的字符串类型改为对象。

在 Vue 中，冒号":" 开头的属性是用来传递数据的，这里的写法的意思就是根据 todo.done 来决定是否有 done 这个 class。

## 进一步优化

- 如何实现在前端页面的列表的最下方，显示一下列表项目中没有完成的项目的比例呢？

  在模板里面写 JS，看起来代码也很乱。
  性能差
  每次渲染都重新执行过滤，而 computed 有缓存，依赖不变则不重算。