- Access log 就像网站的“行车记录仪”：它记下谁（IP）、什么时候、用哪个账号、访问了哪个接口、成功还是出错、花了多少时间。你看这些日志，就能知道系统在干嘛、有没有人乱试密码、哪个接口变慢或挂了——排查问题超有用！
- 应用日志记录系统运行细节：启动流程、数据库连接、请求处理、性能警告（如慢查询）、错误（如空值、死锁）、安全事件（如登录失败）等。INFO 表正常，WARN 提示风险，ERROR 标识故障，是排查问题的关键依据。
- 这些错误日志揭示了系统多个故障点：数据库连接池耗尽导致超时、死锁、代码空值异常、邮件服务不可用、IP频繁请求触发限流、库存不足及Redis连接中断。